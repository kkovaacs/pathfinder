syntax = "proto3";

package starknet.discovery;

message NewNode { bytes signed_peer_record = 1; }

message KnownNodes {
  bytes publicKey = 1;      // id of publishing node
  repeated bytes nodes = 2; // nodes known to the publishing node
}
